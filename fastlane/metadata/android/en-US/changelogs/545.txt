Fixed avatar/profile sizing so communication history rows now obey the dedicated contact/avatar size toggle and percentage, while notification/profile artwork continues to use its own notification/profile size controls. Replaced the experimental multi-endpoint updater with the same one-release GitHub Releases flow used by MarkVault: one /releases/latest request, one fixed APK asset per signing channel, SHA-256 comparison with the installed APK, resumable download, package/signature/version verification, and a 30-second watchdog so update checks cannot remain stuck forever.
