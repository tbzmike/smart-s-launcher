Fixed Vertical List scrolling jank without removing the 3.30.118 icon-pack, avatar-priority or independent size-slider fixes. Notification timeline rows now have their own ListView recycling pool and reuse convertView instead of repeatedly inflating the heavy notification layout; hidden native RemoteViews are not built for History rows; avatar/icon resolution is cached and moved off the UI thread; marquee animation stays paused for both attached and newly rebound rows throughout a fling; unread notification state is cached in memory; and unchanged timestamp/notification text no longer triggers redundant relayouts.
