Reworked in-app updates to match the proven MarkVault design: primary update discovery now uses a normal GitHub Releases manifest, a raw GitHub mirror is the second path, and api.github.com is fallback-only. The manifest carries versionCode, exact APK URL, file size and SHA-256 for both debug and production signing channels before package/signature/version verification and resumable installation.
