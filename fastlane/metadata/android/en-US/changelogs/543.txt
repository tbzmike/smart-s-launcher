Reworked the updater around the actual GitHub Releases channel used by Smart S Launcher. Every latest versioned release now carries fixed SmartSLauncher-debug.apk and SmartSLauncher.apk updater assets. Update checks use the current Android network explicitly and fall back to Android DownloadManager if the launcher's own DNS/socket path fails, then verify package name, signing certificate and version before installation.
