Rebuilt app updating around a dedicated fixed GitHub Release channel, updater-latest, while keeping normal versioned releases for users and F-Droid. Update checks use fixed debug/production APK names, the current Android network explicitly, and Android DownloadManager as a fallback if the launcher process cannot resolve or reach GitHub. Every APK is still verified for package name, signing certificate and version before installation.
